CREATE TABLE temp(
	col01 varchar2(10),
	col02 varchar2(10)
);

SELECT * FROM temp;

INSERT INTO temp (col01, col02)
VALUES ('11111', '22222');

-- 내일은 오라클 내용 복습 진행 계획


-- 3가지의 사건 테이블

CREATE TABLE Mission (
	MISNUM number(10) CONSTRAINT MISSION_MISNUM PRIMARY KEY,
	MISNAME VARCHAR2(20),
	MISDATE DATE
)

CREATE TABLE sudMission ( 
	MISNUM NUMBER(10) CONSTRAINT SUBMISSION_MISNUM REFERENCES MISSION(MISNUM),
	MISNAME VARCHAR2(20),
	MISDATE DATE
);

CREATE TABLE SPEMission ( 
	MISNUM NUMBER(10) CONSTRAINT SPEMISSION_MISNUM REFERENCES MISSION(MISNUM),
	MISNAME VARCHAR2(20),
	MISDATE DATE
);

CREATE TABLE NORMission ( 
	MISNUM NUMBER(10) CONSTRAINT NORMISSION_MISNUM REFERENCES MISSION(MISNUM),
	MISNAME VARCHAR2(20),
	MISDATE DATE
);


SELECT M.MISNUM, M.CODEMISS, M. FROM  
Mission M

ALTER TABLE MISSION
ADD CODEMISS VARCHAR2(20) CONSTRAINT MISSION_CODE_UNIQUE UNIQUE ;

DROP TABLE TEMPEX;

CREATE TABLE TEMPEX (
	COL01 VARCHAR(20) NOT NULL
);

INSERT INTO TEMPEX VALUES (NULL);


CREATE TABLE TEMPEX01 (
	COL01 VARCHAR(20) UNIQUE
);

INSERT INTO TEMPEX01 VALUES ('KEY01');
INSERT INTO TEMPEX01 VALUES (NULL);

SELECT * FROM TEMPEX01;































