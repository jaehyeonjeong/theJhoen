java
camelCase

파이 같은 경우에는 상수 이기 떄문에 PI 라고 작성해야 한다.

css bem

boot strap, tailwindcss 자유도는 높으나 클래스명을 어느 정도 이해를 해야한다.
rem도 단위는 지독하게도 많이 쓰인다.

scss 폴더 내에 _vass.scss로 제작
_ (언더바로 시작하는 scss 파일은 컴파일 되지 않는다.) 즉 css로 만들어지지 않는다.
@import "./vass"; 라고 ubase.scss 파일에 import 해준다.

